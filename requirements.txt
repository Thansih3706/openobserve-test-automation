# Framework dependencies for the Aquanow testing framework
# Versions chosen to match the test environment and plugins observed in the
# project's Allure/pytest reports. Adjust versions as needed.

pytest==8.3.5
requests==2.32.3
python-dotenv==1.0.1

# Reporting & Test plugins
allure-pytest==2.15.0
pytest-html==4.1.1
pytest-timeout==2.4.0
pytest-metadata==3.1.1
pytest-asyncio==1.2.0

# HTTP & mocking libraries used in tests
respx==0.22.0
requests-mock==1.12.1


